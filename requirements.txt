flask>=3.0
requests>=2.28
astropy>=6.0
numpy>=1.24
beautifulsoup4>=4.12
lxml>=5.0
waitress>=3.0
# Password hashing. argon2id is the default and the one we use; the pure-C
# binding means the cost parameters are real rather than aspirational.
argon2-cffi>=23.1
